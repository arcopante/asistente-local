# ============================================================
#  requirements.txt — Dependencias del Agente LM Studio
# ============================================================
#
#  Instalación:
#    pip install -r requirements.txt
#
#  O usando el entorno virtual creado por setup.sh:
#    source .venv/bin/activate
#    pip install -r requirements.txt
# ============================================================

# ── HTTP / API ────────────────────────────────────────────────
# Cliente HTTP async/sync. Usado para comunicarse con LM Studio.
httpx>=0.27.0

# ── Interfaz de terminal ──────────────────────────────────────
# Terminal enriquecido: colores, tablas, paneles, markdown.
rich>=13.7.0

# ── Bot de Telegram ───────────────────────────────────────────
# Framework oficial async para bots de Telegram.
# Incluye httpx como transporte y soporte HTTP/2.
python-telegram-bot[http2]>=21.0

# ── Procesamiento de PDF ──────────────────────────────────────
# Extracción de texto de ficheros PDF enviados por Telegram.
pypdf>=4.0.0
faster-whisper>=1.0.0
